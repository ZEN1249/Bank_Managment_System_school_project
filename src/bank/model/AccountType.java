package bank.model;

/*
 * Nazwa: AccountType
 * Parametry: brak
 * Opis: Enum definiujący podstawowe typy kont bankowych dostępne w systemie.
 */
public enum AccountType {
    SAVINGS,
    CHECKING
}